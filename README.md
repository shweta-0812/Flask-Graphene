# Flask-GraphQL

## Project Setup

```
1. To connect to Mysql server use:
    mysql -u USERNAME -pPASSWORD -h HOSTNAMEORIP DATABASENAME 
in terminal of mysql docker container either from command line or docker dashboard
2.       DB_HOST: database - is the same name as of the db service used in docker-compose file to run db    
```